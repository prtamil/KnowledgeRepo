what is -i option means ? what's it usage ?
------------------------------------------

-i => interface
which interface to capture packets any.

$ tcpdump -i lo   #captures packet from loopback device interface
