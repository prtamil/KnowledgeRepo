app.get('/string',fn) -> String contains str, +?*() as regex. 
                      -> Takes .,- as string litrel

app.get(/regex/,fn)  -> Takes complete regex as path 
                     -> Note no string in path
